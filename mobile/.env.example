API_URL=https://bakeryapp-backend-l87q.onrender.com
# Mobile (Expo) example environment variables
# Copy values into your local env handling (or `app.config.js`) and do NOT commit real secrets.

# Public API URL that the mobile app uses to reach the backend
# Replace 192.168.1.8 with your machine LAN IP when testing on a physical device
EXPO_PUBLIC_API_URL=http://192.168.1.8:5000/api

# Optional: websocket or socket url if used
EXPO_PUBLIC_WS_URL=

# Expo-specific keys (do NOT store private keys here for production)
EXPO_PUBLIC_ANALYTICS_KEY=

NODE_ENV=development
