# Backend example environment variables
# Copy this file to `.env` and replace values before running locally.

# Postgres connection string
# Example: postgres://user:password@localhost:5432/bakerydb
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE

# JWT signing secret
JWT_SECRET=your_jwt_secret_here

# JWT refresh secret (optional, can be same as JWT_SECRET)
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here

# Server port (optional)
PORT=5000

# Node environment
NODE_ENV=development

# Expo Push Notification endpoint (for mobile push)
EXPO_PUSH_ENDPOINT=https://exp.host/--/api/v2/push/send

# Optional external API base URL (if used by backend)
EXTERNAL_API_URL=

# NOTE: Redis/Upstash is no longer required for notifications.
# Notifications now use Prisma/Postgres directly for better reliability.
